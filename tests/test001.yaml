openapi: 3.0.0
components:
  schemas:
    TestRequestType:
      type: object
      properties:
        IntField:
          type: integer
    TestResponseType:
      type: object
      properties:
        IntField:
          type: integer
info:
  title: test001.yaml
  version: 0.0.0
paths:
  /test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestRequestType'
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestResponseType'
        default:
          description: ""
